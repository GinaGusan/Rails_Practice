<h1>All comments</h1>

<% @comments.each do |c| %>
	<p> <%= link_to c.id, {:controller => :comments, :action => :show, :id => c.id}  %> </p>
<% end %>

<%= link_to 'Add new comment', new_comment_path %>